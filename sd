<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fishing Game</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #87CEEB;
        }
        .button-container {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin: 20px 0;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #0077b6;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:disabled {
            background-color: gray;
            cursor: not-allowed;
        }
        #status {
            font-size: 18px;
            font-weight: bold;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Fishing Game 🎣</h1>
    <p id="coins">💰 Coins: 0</p>
    <p id="fishCaught">🐟 Fish Caught: 0</p>
    <div class="button-container">
        <button id="fishButton">🎣 Start Fishing</button>
        <button id="upgradeButton">⚡ Upgrade Rod (50 Coins)</button>
    </div>
    <p id="status">Welcome to the fishing game!</p>

    <script>
        class FishingGame {
            constructor() {
                this.fishCaught = 0;
                this.isFishing = false;
                this.rodLevel = 1;
                this.coins = 0;
                this.fishTypes = [
                    { name: "Salmon", value: 5 },
                    { name: "Tuna", value: 10 },
                    { name: "Shark", value: 20 },
                    { name: "Golden Fish", value: 50 }
                ];
                this.bindButtons();
                this.updateUI();
            }

            bindButtons() {
                document.getElementById("fishButton").onclick = () => this.startFishing();
                document.getElementById("upgradeButton").onclick = () => this.upgradeRod();
            }

            startFishing() {
                if (this.isFishing) return;
                this.isFishing = true;
                document.getElementById("fishButton").disabled = true;
                this.updateStatus("Casting the fishing line... 🎣");

                setTimeout(() => {
                    this.catchFish();
                }, 2000 - this.rodLevel * 200);
            }

            catchFish() {
                this.isFishing = false;
                let fish = this.getRandomFish();
                this.fishCaught++;
                this.coins += fish.value;
                this.updateStatus(`You caught a ${fish.name}! 💰 +${fish.value} Coins`);
                this.updateUI();
                document.getElementById("fishButton").disabled = false;
            }

            upgradeRod() {
                if (this.coins < 50) {
                    this.updateStatus("Not enough coins to upgrade!");
                    return;
                }
                if (this.rodLevel >= 5) {
                    this.updateStatus("Rod is already max level!");
                    return;
                }
                this.coins -= 50;
                this.rodLevel++;
                this.updateStatus(`Rod upgraded to level ${this.rodLevel}!`);
                this.updateUI();
            }

            getRandomFish() {
                return this.fishTypes[Math.floor(Math.random() * this.fishTypes.length)];
            }

            updateUI() {
                document.getElementById("coins").innerText = `💰 Coins: ${this.coins}`;
                document.getElementById("fishCaught").innerText = `🐟 Fish Caught: ${this.fishCaught}`;
                document.getElementById("upgradeButton").disabled = this.coins < 50;
            }

            updateStatus(message) {
                document.getElementById("status").innerText = message;
            }
        }

        const game = new FishingGame();
    </script>
</body>
</html>
